# Working option slices

These CSVs are the exact inputs used in the paper. All four are the Cboe session of 23 September 2026. Each file has a `#` metadata line (`S0`, `r`, `q`, `T`, `F`, expiry, asof, root) and columns `K,P,C,source`.

They are a research extract of Cboe delayed quotes, not a redistribution of the full chain. See the repository `README.md`.
